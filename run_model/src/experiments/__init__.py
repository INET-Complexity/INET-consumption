"""Reusable experiment runners used by notebooks and compatibility commands.

Submodules are deliberately not imported here: MPC, IRF, and Monte Carlo have
different optional dependencies and should remain independently importable.
"""
